function partyBonusUsed(member){return Object.keys(member.stats).reduce((total,key)=>total+(member.stats[key]-member.baseStats[key]),0)}
function synchronizeBattleMember(member,classChanged=false){const classId=member.classId,base={...GAME_CONTENT.classBases[classId]};if(classChanged){member.baseStats=base;member.stats={...base};member.abilities=[GAME_CONTENT.specialPassiveNames[classId]]}else{member.baseStats={...base,...(member.baseStats||{})};Object.keys(base).forEach(key=>member.stats[key]=Math.max(member.baseStats[key],member.stats[key]??member.baseStats[key]));let excess=partyBonusUsed(member)-member.level;for(const key of Object.keys(member.stats).reverse()){if(excess<=0)break;const available=member.stats[key]-member.baseStats[key],reduction=Math.min(available,excess);member.stats[key]-=reduction;excess-=reduction}}const special=GAME_CONTENT.specialPassiveNames[classId],chosen=(member.abilities||[]);member.specialSelected=chosen.includes(special);const extraLimit=member.level<3?0:member.level<6?1:member.level<9?2:3;member.selectedPassives=chosen.filter(ability=>ability!==special).slice(0,extraLimit);member.abilities=[...(member.specialSelected?[special]:[]),...member.selectedPassives]}
party.forEach(member=>synchronizeBattleMember(member));render();
document.querySelector('#party-app').addEventListener('change',event=>{const member=party.find(item=>item.id===selectedId);if(!member)return;const element=event.target;const classChanged=element.matches('[data-field="classId"]');if(element.matches('[data-field="level"], [data-stat], [data-ability], [data-field="classId"]')){synchronizeBattleMember(member,classChanged);dirty=true;render()}});
